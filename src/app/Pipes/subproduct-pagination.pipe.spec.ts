import { SubproductPaginationPipe } from './subproduct-pagination.pipe';

describe('SubproductPaginationPipe', () => {
  it('create an instance', () => {
    const pipe = new SubproductPaginationPipe();
    expect(pipe).toBeTruthy();
  });
});
