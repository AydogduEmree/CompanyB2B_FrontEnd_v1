import { PriceBookFilterPipe } from './price-book-filter.pipe';

describe('PriceBookFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new PriceBookFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
